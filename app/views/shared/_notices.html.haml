-# This needs some padding, but only when on certain pages.
-# Maybe it's best to have two notices partials:
-# one with padding and one without.
- if flash[:error]
  #error_explanation.error
    -# = pluralize(object.errors.count, 'error')
    -# prevented this
    -# = object.class.to_s.underscore.humanize.downcase
    -# from being saved:
    %ul
      - flash[:error].each do |msg|
        %li= msg

- flash.each do |k,v|
  - next if k == :error
  <div class="#{k} flash">#{v}</div>
