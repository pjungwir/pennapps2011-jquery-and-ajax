%h1 First Problem: Ready Handler

We want to run some Javascript<br/>
after the page is fully loaded.

%ul
  %li Necessary for UJS.
  %li Earliest we can access the DOM.
  %li This is too late:

%pre{:class => 'brush: js'}
  :preserve
  :escaped
    window.onload = function() {
      // ...
    }

