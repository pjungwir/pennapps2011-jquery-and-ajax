%pre{:class => 'brush: js'}
  :preserve
  :escaped
    function Dog(theName) {
      this.name = theName;

      this.speak = function() {
        alert(this.name + ' says "Woof!"');
      }

      this.fetch = function() {
        // ...
      }
    }

    var fido = new Dog("Fido");
    fido.speak();


