source 'http://rubygems.org'

gem 'rails', '3.0.3'

gem 'pg'

gem 'compass'
gem 'haml', '3.1.1'

gem 'json'

group :development do
  gem 'silent-postgres'         # Maybe in production too??
  gem 'annotate', '~> 2.4.0'  # Run `annotate` to add comments to models
                                  # describing their tables.
  gem 'rspec-rails', '2.5.0'  # Provides command-line generators for rspec.
end
